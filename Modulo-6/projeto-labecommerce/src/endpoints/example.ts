//aqui ficara a função
import express, { Response, Request } from "express";
import {connection} from "../connection"

export async function getallproducts (
    req: Request, 
    res: Response
    ): Promise<void>{
        try {
            
        } catch (error:any) {
            
        }
    }